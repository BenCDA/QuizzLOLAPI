document.addEventListener('DOMContentLoaded', () => {
    // Create our variables
    const searchInput = document.getElementById('search-input');
    const spellList = document.getElementById('spell-list');
    const spellDetails = document.getElementById('spell-details');
    const spellName = document.getElementById('spell-name');
    const spellDetailsImage = document.getElementById('spell-details-image');
    const spellSkills = document.getElementById('spell-skills');
    const spellStats = document.getElementById('spell-stats');
    const spellModalName = document.getElementById('spell-modal-name');
    const spellModalImage = document.getElementById('spell-modal-image');
    const spellModalSkills = document.getElementById('spell-modal-skills');
    const spellModalStats = document.getElementById('spell-modal-stats');
    const closeBtn = document.getElementsByClassName('close') [0];

    let spells = [];
})